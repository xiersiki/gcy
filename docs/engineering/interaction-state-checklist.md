# 交互状态设计清单

## 异步状态

- 每个异步操作都定义：`idle/loading/success/error`
- 错误可恢复：必须提供重试或回退路径
- 并发策略明确：`last-write-wins` 或串行锁

## 乐观更新

- 先更新 UI，再发请求
- 请求失败必须完整回滚
- 回滚后提示用户后续动作（重试/登录）

## URL 状态

- 影响用户定位的状态写入 URL（筛选、tab）
- 非法参数有明确兜底默认值
- 回退/刷新后状态一致
